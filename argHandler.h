#ifndef ARGHANDLER_H 
#define ARGHANDLER_H  

int hasProperArgs(int);  // function that will take the argument count as a parameter and -
			 // - return weather or not there were proper command line arguments supplied.
#endif
